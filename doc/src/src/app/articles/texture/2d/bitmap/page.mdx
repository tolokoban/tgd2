import Demo from "./_"

# Texture2D from bitmap

We call Bitmap, an image, a video, a canvas, ... All the following types,
actually: `ImageData`, `HTMLImageElement`, `HTMLCanvasElement`,
`HTMLVideoElement` and `ImageBitmap`.

This is the easiest way to import data into a texture.

<Demo />

Youla!
