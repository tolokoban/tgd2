import Column from "@/components/Column"
import Ratio from "./_/Ratio.tsx"
import Zoom from "./_/Zoom.tsx"

# TgdPainterBackground

Ce painter dessine un arrière-plan en conservant son aspect ratio. Par defaut,
l'image couvre l'écran. Exemples :

<Column align="full">
    <Ratio />
</Column>

Les paramètres suivants sont aussi disponibles :

-   **`zoom`** : La valeur **1** correspond à la plus petite image recouvrant
    entièrement la scène.

<Zoom />
