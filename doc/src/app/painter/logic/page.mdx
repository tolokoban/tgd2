import Demo from "./_/main.demo"

# TgdPainterLogic

Une fonction qui s'exécute à chaque frame.

## Exemple

Utilisez les touches `R`, `G` et `B` (avec ou sans `shift`) pour faire varier la
couleur.

<Demo />
