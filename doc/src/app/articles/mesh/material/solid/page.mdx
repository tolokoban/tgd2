import Demo from "./_material"

# Solid material

The difference with `TgdMaterialFlat` is that you can change the color once
the `TgdMaterialSolid` is created.
Which is impossible with `TgdMaterialFlat`.

So **Flat** is faster than **Solid**, but less convenient.

<Demo />

---

[Back](#/articles/mesh/material)
