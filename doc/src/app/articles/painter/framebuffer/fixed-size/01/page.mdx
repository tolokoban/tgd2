import Demo from "./_/framebuffer.demo"

# [TgdPainterFramebuffer](docs/classes/TgdPainterFramebuffer.html) - Fixed size

In this demo we will create a textured rotating cube, then use it to texture another cube.

So let's start with the first cube, without using framebuffers.
We will create a new Painter, to make it easier for the next steps.

<Demo />

---

[Next step](#/articles/painter/framebuffer/fixed-size/02)
