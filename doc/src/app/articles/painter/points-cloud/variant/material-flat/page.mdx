import DemoSimple from "./_/points-cloud.demo"
import DemoMany from "./_/many.demo"

# Points Clouds

## Flat shading

With flat shading, we paint disks instead of spheres.
That will speed up the rendering a lot and can be used in offscreen canvases to implement object picking technique.

### Few spheres 

<DemoSimple />

### Many spheres

Here, we display 503'808 spheres.
It's way smoother than with the full material.

<DemoMany />